{% comment %} This is to show project cards in the project-list.liquid {% endcomment %}

<a class="project" href="{{ project.url }}">
  <div class="project-image">
    <img loading="lazy"
      srcset="{% if project.featured_image.width >= 330 %}{{ project.featured_image | image_url: width: 300 }} 300w,{% endif %}
              {% if project.featured_image.width >= 450 %}{{ project.featured_image | image_url: width: 350 }} 350w,{% endif %}"
      src="{{ project.featured_image | image_url: width: 350 }}"
      sizes="(min-width: 1400px) 350px, (min-width: 1200px) 350px, (min-width: 992px) 350px"
      alt="{{ project.title }}" 
      height="{{ project.featured_image.height }}" 
      width="{{ project.featured_image.width }}"
    >
  </div>
  <div class="project-content">
    <span>{{ project.title }}</span>
  </div>
</a>
